name: Secret and Sensitive Data Scanning

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  secret-scanning:
    uses: bajor/github-workflows/.github/workflows/secret-scanning.yml@main
